%h1='刷卡'
%p='此金流為使用 Braintree Sandbox，並非真付款'
%p='Card Number 可用 4111 1111 1111 1111'
%p='Expiration Date可自己打'

= form_tag payment_path, method: :post do |f|
  %div{ id: "dropin" }
  %input{ type: "submit", value: "#{t("pay")}", class: "btn btn-primary" }

:javascript
  braintree.setup(
    "#{@client_token}",
    'dropin', {
      container: 'dropin'
    });